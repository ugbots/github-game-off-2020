const colors = require('tailwindcss/colors');

module.exports = {
  theme: {
    colors: {
      red: colors.red,
      gray: colors.coolGray,
      green: colors.emerald,
      purple: colors.purple,
    },
    fontFamily: {
      mono: ['monospace'],
    },
  },
};
